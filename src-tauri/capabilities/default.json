{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Everything the viewer window is allowed to do. Deliberately minimal: no filesystem plugin, no HTTP plugin, no shell. The only network access is the webview's own fetch to api.github.com, allowed by the CSP in tauri.conf.json rather than by a capability.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:window:allow-show",
    "core:webview:allow-internal-toggle-devtools",
    "dialog:allow-open",
    "dialog:allow-save",
    {
      "identifier": "opener:allow-open-url",
      "allow": [
        { "url": "http://*" },
        { "url": "https://*" },
        { "url": "mailto:*" },
        { "url": "tel:*" },
        { "url": "file://*" }
      ]
    },
    "core:app:allow-version"
  ]
}
